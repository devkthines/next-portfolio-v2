'use client'

import { ClockCircleOutlined } from '@ant-design/icons';
import { Timeline } from 'antd';

export default function Resume(){
 onst items = [{ children: 'sample', label: 'sample' }];   return(
        <>
 c
<Timeline items={items} />;
    )
}